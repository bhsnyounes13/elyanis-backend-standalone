# EL-YANIS — variables d’environnement (exemple, sans secrets réels)
#
# Copier vers `.env` à côté de `package.json`.
# Prisma lit `DATABASE_URL` via `prisma/schema.prisma` → datasource `db`.
#
# Développement : PostgreSQL + `npx prisma db push` (ou `db:seed`).

# ---------------------------------------------------------------------------
# Prisma / PostgreSQL (obligatoire)
# ---------------------------------------------------------------------------
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE_NAME?schema=public"
# DB_CONNECT_TIMEOUT_MS=30000
# DIRECT_URL="..."

# ---------------------------------------------------------------------------
# API
# ---------------------------------------------------------------------------
NODE_ENV=development
PORT=3000
# SERVE_SPA=true

# ---------------------------------------------------------------------------
# Obligatoires au démarrage (dev et production)
# ---------------------------------------------------------------------------
JWT_ACCESS_SECRET=change-me-minimum-32-characters-long-secret!!
FRONTEND_ORIGIN=http://localhost:8080

# ---------------------------------------------------------------------------
# Production (validation au démarrage)
# ---------------------------------------------------------------------------
# TURNSTILE_SECRET_KEY=

# ---------------------------------------------------------------------------
# Turnstile / formulaires
# ---------------------------------------------------------------------------
FORMS_REQUIRE_TURNSTILE=false
# JWT_ACCESS_EXPIRES=15m
# JWT_REFRESH_DAYS=7
# BCRYPT_ROUNDS=12
# REFRESH_COOKIE_NAME=ely_refresh
# BOOTSTRAP_ADMIN_EMAIL=admin@example.com
# BOOTSTRAP_ADMIN_PASSWORD=
# LOG_LEVEL=info
# SENTRY_DSN=
# SENTRY_TRACES_SAMPLE_RATE=0.1
# SENTRY_RELEASE=elyanis-api@1.0.0

# ---------------------------------------------------------------------------
# Stockage (uploads)
# ---------------------------------------------------------------------------
# STORAGE_LOCAL_ROOT=data/uploads
# STORAGE_LOCAL_PUBLIC_BASE=https://votredomaine.com
# STORAGE_BUCKET=
# STORAGE_REGION=us-east-1
# STORAGE_ENDPOINT=
# STORAGE_ACCESS_KEY_ID=
# STORAGE_SECRET_ACCESS_KEY=
# STORAGE_PUBLIC_URL=
# STORAGE_FORCE_PATH_STYLE=false
